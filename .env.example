# Keep false for the standalone visual demo. Set true when running with peper24-server.
VITE_USE_SERVER_API=false
# Keep empty in local development so Vite proxies /api to peper24-server.
VITE_API_BASE_URL=
